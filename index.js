function theBeatlesPlay(musicians, instruments) {
  var array = []
  for (let i = 0; i < musicians.length; ) {
    array.push('${musicans[i]} plays ${instruments[i]}')
  }
  return array
}

function johnLennonFacts(facts) {
  
}